//Notes: if someday u need to add another icon for floating banner
import { ArrowForward } from "@material-ui/icons";

const Icon = { ArrowForward: null };
Icon.ArrowForward = <ArrowForward />;

export default Icon;
